#!/bin/bash

function enable_limited_functionality() {
  touch '<%= p('uaa.limitedFunctionality.statusFile', '/tmp/uaa-non-existent') %>'
}

enable_limited_functionality
#uaa checks the file system every 5 seconds
sleep 6
